{
	"name": "Battle Music",
	"image": "mcr.microsoft.com/devcontainers/java:3-25-trixie",
	"customizations": {
		"vscode": {
			"extensions": [
				"vscjava.vscode-java-pack",
				"vscjava.vscode-gradle"
			]
		}
	},
	"postCreateCommand": "curl -fsSL https://opencode.ai/install | bash && sudo apt update && sudo apt install -y git-lfs"
}
